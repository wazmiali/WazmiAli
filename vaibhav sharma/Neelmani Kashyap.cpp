#include<stdio.h>
int main()
{
	int n,fact=1,num;
	printf("enter the value of n");
	scanf("%d",&n);
	if(n<1)
	printf("number is negative and its factorial is not possible");
	else
	{
		while (num>1);
		fact=fact*num;
		n--;
	}
	printf("fact of -ve number does not exist");
	return 0;
}
